/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.shutterstock.com', 'images.oyoroomscdn.com','media.istockphoto.com'],
    }
};

export default nextConfig;
